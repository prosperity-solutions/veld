# Packaging config for Veld Desktop (see desktop/ARCHITECTURE.md → "Packaging").
#
# macOS and Linux only. There is deliberately no Windows target: the whole UI is
# served by the daemon and a plain browser covers that platform, so a Windows
# build would be an installer around a page you can already open.
#
# The version comes from package.json, which release CI rewrites to the tag being
# built — the app and the CLI ship one version from one tag (see
# `.releaserc.json` and `.github/workflows/release.yml`).
appId: dev.veld.desktop
productName: Veld
copyright: Copyright © Prosperity Solutions

# `buildResources` is deliberately left at its default (`build/`, which does not
# exist here): electron-builder excludes that directory from the packaged app, and
# `assets/` has to ship — the tray icon is read from it at runtime. The icons are
# referenced by explicit path below instead.
directories:
  output: dist

# An allow-list, not an ignore-list: the shell is four source files plus the
# generated icons, and anything else in here (scripts/, tests, dist/) has no
# business inside the bundle. Production `dependencies` are added implicitly.
files:
  - src/**/*.js
  - "!src/**/*.test.js"
  - assets/**
  - package.json

# `latest-mac.yml` / `latest-linux.yml` are the update feed electron-updater
# reads, and they are only written when a publish provider is configured — so
# this block exists even though CI always packages with `--publish never` and
# attaches the artifacts through the release workflow's own `publish` job,
# alongside the CLI tarballs. It is also what gets baked into the bundle as
# `app-update.yml`, which is how the installed app knows where to look.
publish:
  provider: github
  owner: prosperity-solutions
  repo: veld

mac:
  category: public.app-category.developer-tools
  # 1024×1024 PNG generated by `scripts/make-icons.py`; electron-builder derives
  # the .icns from it, so there is no second committed asset to drift.
  icon: assets/icon.png
  target:
    - target: dmg
      arch: [arm64, x64]
    # The zip is what Squirrel.Mac updates *from* — a dmg is a disk image, not an
    # update payload — so `latest-mac.yml` points at it. Shipped now even though
    # macOS self-install is off (see below), because the alternative is a release
    # whose update feed describes files that aren't there.
    - target: zip
      arch: [arm64, x64]
  # No Developer ID yet (issue #167 §10). electron-builder's own signing is
  # skipped explicitly rather than left to auto-discovery — otherwise the
  # artifacts depend on which certificates happen to be in the keychain of the
  # machine that built them, which is the one property a release must not have.
  # `scripts/adhoc-sign.js` then ad-hoc signs the bundle, which is not a
  # trust signal but *is* what makes it launch at all on Apple Silicon.
  identity: null
linux:
  category: Development
  icon: assets/icon.png
  maintainer: Prosperity Solutions <veld@life.li>
  synopsis: Local development environment orchestrator
  target:
    # AppImage first: it is the only Linux format electron-updater can install
    # in place, so it is the one that gets self-updates. The .deb is for people
    # who want it in their package manager and updates through the same download
    # -a-new-one path macOS is on today.
    - target: AppImage
      arch: [x64]
    - target: deb
      arch: [x64]

# `${arch}` is electron's spelling (x64), not the release tarballs' (amd64).
# Left as-is: renaming it here would make the file name disagree with the update
# feed electron-builder writes beside it, which is the thing that has to match.
artifactName: Veld-${version}-${os}-${arch}.${ext}

afterPack: scripts/adhoc-sign.js
