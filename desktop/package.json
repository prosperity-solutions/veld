{
  "name": "@veld/desktop",
  "private": true,
  "version": "0.0.0",
  "description": "Veld Desktop — Electron shell around the veld management UI (/ide)",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test src/*.test.js"
  },
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
